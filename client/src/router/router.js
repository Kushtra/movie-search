import { moviesPage, homePage, loginPage, registerPage } from '@/constants';

export class Pages {
  movies = { name: moviesPage };
  login = { name: loginPage };
  register = { name: registerPage };
  home = { name: homePage };
}
